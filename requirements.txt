# ============================================
# 中科大智能问答助手 - 依赖清单
# 环境: Python 3.12, CUDA 12.8
# 注意: AutoDL 镜像已自带 PyTorch，无需重复安装
# ============================================

# --- 基础依赖 (不指定 torch，使用镜像自带版本) ---
transformers>=4.46.0
datasets>=3.0.0
accelerate>=1.0.0
peft>=0.13.0
trl>=0.12.0

# --- 分布式训练 ---
deepspeed>=0.15.0

# --- 量化 ---
auto-gptq>=0.7.0
autoawq>=0.2.0
optimum>=1.23.0

# --- 推理部署 ---
vllm>=0.6.0
openai>=1.50.0

# --- 数据工程 ---
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# --- 前端界面 ---
gradio>=5.0.0

# --- 工具 ---
tqdm>=4.66.0
pandas>=2.2.0
numpy>=1.26.0

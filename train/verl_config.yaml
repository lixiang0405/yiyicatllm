# ============================================
# veRL GRPO 训练配置（纯覆盖文件，不使用 defaults）
# 通过 run_grpo.sh 中 --config-name=ppo_trainer 加载 veRL 内置默认配置
# 本文件的字段会覆盖默认值
# ============================================
